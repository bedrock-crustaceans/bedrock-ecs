package bedrock-ecs:plugin;

interface server {
    get-version: func() -> list<u32>;
}

interface metadata {
    record manifest {
        name: string,
        version: list<u32>
    }

    get-manifest: func() -> manifest;
}

world guest {
    import server;
    export metadata;
}
